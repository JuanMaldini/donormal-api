PB_URL=
PB_TOKEN=
PB_USERS=clothfigurator_user
PB_DATA=clothfigurator_data

# (opcional) Token para activar el modo admin del dashboard (ver /api/admin/*).
# Si está vacío, el modo admin no está disponible.
# Generá uno con:  python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_TOKEN=
